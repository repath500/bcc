{
  "name": "@inspect/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "lint": "eslint . --ignore-pattern '.wrangler'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@inspect/github-integration": "workspace:*",
    "@inspect/shared": "workspace:*",
    "hono": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "wrangler": "^3.62.0"
  }
}
